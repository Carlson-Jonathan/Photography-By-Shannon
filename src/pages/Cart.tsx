import { Box, Typography } from "@mui/material";
import * as styles from '../styles/About';

const Cart = () => {

  return (
    <Box sx={styles.generic}>
      <Typography>
        Cart
      </Typography>
    </Box>
  );
};

export default Cart;