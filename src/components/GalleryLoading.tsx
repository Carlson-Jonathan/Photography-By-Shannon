import { Box, Typography } from "@mui/material";

export const GalleryLoading = () => {
  return (
    <Box>
      <Typography>Loading images. Please wait...</Typography>
    </Box>
  );
};

export default GalleryLoading;
